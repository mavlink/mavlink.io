import{_ as a,c as t,a8 as o,o as d}from"./chunks/framework.sDsl5pD6.js";const u=JSON.parse('{"title":"Standard Modes Protocol (WIP)","description":"","frontmatter":{},"headers":[],"relativePath":"en/services/standard_modes.md","filePath":"en/services/standard_modes.md"}'),i={name:"en/services/standard_modes.md"};function s(n,e,r,m,l,h){return d(),t("div",null,e[0]||(e[0]=[o('<h1 id="standard-modes-protocol-wip" tabindex="-1">Standard Modes Protocol (WIP) <a class="header-anchor" href="#standard-modes-protocol-wip" aria-label="Permalink to &quot;Standard Modes Protocol (WIP)&quot;">​</a></h1><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>This protocol is a work in progress and may change. It has an initial implementation in PX4 and QGroundControl (daily).</p></div><p>The MAVLink standard modes protocol allows a GCS to construct a UI allowing selection and display of available flight modes without prior knowledge of the connected flight stack.</p><p>The protocol provides:</p><ul><li><p>Definitions of standard modes. These are modes that behave similarly enough across flight stacks that users can select and control them in the same way. For example, modes for taking off and landing, executing missions, holding position, and so on.</p></li><li><p>Enumeration of all flight modes supported by the flight stack, both standard and custom.</p><p>The provided flight mode information includes:</p><ul><li>Flags indicating whether particular custom modes can be selected or just displayed, and if they are displayed, whether they are advanced.</li><li>A name/metadata string that can either be used to fetch metadata for the mode (such as a name and description), or directly as the name for custom modes.</li></ul></li><li><p>Notification of the current mode.</p></li><li><p>A command to set a standard mode.</p></li></ul><p>There are additional optional messages/features to allow:</p><ul><li>Modes to be enabled and disabled dynamically.</li><li>Overriding of mode metadata (provide a metadata key).</li></ul><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>MAVLink previously only standardized very high level <em>base modes</em> (defined in <a href="./../messages/common.html#MAV_MODE">MAV_MODE</a>), which cover things like whether the vehicle is ready to fly, being tested, manually controlled, under GCS manual control, or executing a mission. More specific flight behaviour/modes are defined in <em>custom modes</em>, which are specific to each flight stack.</p><p>Mechanisms are provided to set the base and custom modes (<a href="./../messages/common.html#MAV_CMD_DO_SET_MODE">MAV_CMD_DO_SET_MODE</a>), and the <code>HEARTBEAT</code> contains the current/active <code>base_mode</code> and <code>custom_mode</code>. However, prior to the addition of this service, there were no mechanisms query the available custom modes, so a GCS would have to know about a flight stack in advance in order to construct its mode display and setting UI.</p><p>Most autopilots implement a set of custom flight modes that have very similar behaviour or intent, for example: position-hold mode, altitude-hold mode, mission mode, safety return/RTL. However because these are all identified by different custom mode identifiers on different flight stacks, there is no way to be sure what these &quot;mean&quot; without pre-existing knowledge.</p><p>This service defines standard modes, allowing a GCS to provide baseline support for common flight behaviour and test behaviour without explicit customisation for each autopilot. This is therefore a big step towards MAVLink being capable of being truly autopilot-agnostic for a useful minimal set of functionality.</p><p>For more information see <a href="https://github.com/mavlink/rfcs/blob/master/text/0016-standard-modes.md" target="_blank" rel="noreferrer">MAVLink RFC0016 Standard modes</a>.</p><h2 id="message-enum-summary" tabindex="-1">Message/Enum Summary <a class="header-anchor" href="#message-enum-summary" aria-label="Permalink to &quot;Message/Enum Summary&quot;">​</a></h2><h3 id="messages" tabindex="-1">Messages <a class="header-anchor" href="#messages" aria-label="Permalink to &quot;Messages&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Message</th><th>Description</th></tr></thead><tbody><tr><td><a id="CURRENT_MODE"></a><a href="./../messages/development.html#CURRENT_MODE">CURRENT_MODE</a></td><td>Get information about the current active mode, including both its standard and custom mode identifiers (if defined). Also &quot;the intended mode&quot;, to indicate the case where setting a mode failed.</td></tr><tr><td><a id="AVAILABLE_MODES"></a><a href="./../messages/development.html#AVAILABLE_MODES">AVAILABLE_MODES</a></td><td>Get information about all available modes (both standard and custom) allowing generation of a UI and setting of modes. Requested using <a href="./`MAV_CMD_REQUEST_MESSAGE`.html">MAV_CMD_REQUEST_MESSAGE</a> (set param2=0 for all modes).</td></tr><tr><td><a id="AVAILABLE_MODES_MONITOR"></a><a href="./../messages/development.html#AVAILABLE_MODES_MONITOR">AVAILABLE_MODES_MONITOR</a></td><td>Provides a sequence number that allows clients to determine if the set of available modes has changed, and hence whether the <a href="#AVAILABLE_MODES">AVAILABLE_MODES</a> must be re-requested. Sent on first mode change and subsequently streamed at low rate.</td></tr></tbody></table><h3 id="enums" tabindex="-1">Enums <a class="header-anchor" href="#enums" aria-label="Permalink to &quot;Enums&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Enum</th><th>Description</th></tr></thead><tbody><tr><td><a id="MAV_STANDARD_MODE"></a><a href="./../messages/development.html#MAV_STANDARD_MODE">MAV_STANDARD_MODE</a></td><td>Identifiers for the standard modes (modes that have a well understood meaning across flight stacks and vehicle types). Used in <a href="#AVAILABLE_MODES">AVAILABLE_MODES</a> and <a href="#CURRENT_MODE">CURRENT_MODE</a>.</td></tr><tr><td><a id="MAV_MODE_PROPERTY"></a><a href="./../messages/development.html#MAV_MODE_PROPERTY">MAV_MODE_PROPERTY</a></td><td>Properties of a mode supplied in <a href="#AVAILABLE_MODES">AVAILABLE_MODES</a>, which may be used for GCS layout. For example <code>MAV_MODE_PROPERTY_NOT_USER_SELECTABLE</code> indicates that a GCS should not include the mode in UI elements that allow mode selection.</td></tr><tr><td></td><td></td></tr></tbody></table><h3 id="commands" tabindex="-1">Commands <a class="header-anchor" href="#commands" aria-label="Permalink to &quot;Commands&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Command</th><th>Description</th></tr></thead><tbody><tr><td><a id="MAV_CMD_DO_SET_STANDARD_MODE"></a><a href="./../messages/development.html#MAV_CMD_DO_SET_STANDARD_MODE">MAV_CMD_DO_SET_STANDARD_MODE</a></td><td>Enable the specified standard MAVLink mode (<a href="#MAV_STANDARD_MODE">MAV_STANDARD_MODE</a>). ACK with <a href="./../messages/development.html#MAV_RESULT_FAILED">MAV_RESULT_FAILED</a> if the mode is not supported by the vehicle.</td></tr><tr><td><a id="MAV_CMD_REQUEST_MESSAGE"></a><a href="./../messages/common.html#MAV_CMD_REQUEST_MESSAGE">MAV_CMD_REQUEST_MESSAGE</a></td><td>Used to request <a href="#AVAILABLE_MODES">AVAILABLE_MODES</a> message</td></tr></tbody></table><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to &quot;Operations&quot;">​</a></h2><h3 id="getting-available-modes" tabindex="-1">Getting Available Modes <a class="header-anchor" href="#getting-available-modes" aria-label="Permalink to &quot;Getting Available Modes&quot;">​</a></h3><p>All available modes can be enumerated by using <a href="#MAV_CMD_REQUEST_MESSAGE">MAV_CMD_REQUEST_MESSAGE</a> to request the <a href="#AVAILABLE_MODES">AVAILABLE_MODES (435)</a> message. Each <code>AVAILABLE_MODES</code> message includes the total number of modes and the index of the current mode, so a GCS should first query for all modes (<code>MAV_CMD_REQUEST_MESSAGE.param2=0</code>), and then individually query for any <code>AVAILABLE_MODES</code> message that it missed (<code>MAV_CMD_REQUEST_MESSAGE.param2=&quot;missing index number&quot;</code>).</p><p>Notes:</p><ul><li><code>AVAILABLE_MODES</code> includes the total number of modes and the index of the current mode. This indexing is provided to allow a GCS to confirm that all modes have been collected, and re-request any that are missing. It is internal and should not be relied upon to have any order between reboots or if the set of available modes changes.</li><li>The GCS can request this message using <code>MAV_CMD_REQUEST_MESSAGE</code>, specifying either &quot;send all modes&quot; or &quot;send mode with this index&quot;.</li><li>The flight stack should only emit a single <code>AVAILABLE_MODES</code> for each mode on each request. If a mode is both custom and standard it should be emitted as a &quot;standard mode&quot; (this allows the GCS to list the &quot;standard modes&quot; and separately show the additional &quot;custom modes&quot;).</li><li>The modes that are emitted depend on the current vehicle type. For example, &quot;takeoff&quot; would not be emitted for a rover type, but would for a copter.</li></ul><p>The sequence for requesting all modes is shown below:</p><p><a href="https://mermaid-js.github.io/mermaid-live-editor/edit#pako:eNqllF9L5DAUxb_KJU8t1EGd9aVSobZBZKf-mY4-FUpsb8dAk8wmKbsifnfTzIzuuLusOH0o4XLuOT-SmzyTRrVIYmLwx4CywZyzpWbitJLgvhXTljd8xaSFi6z8s5hrJXFdvlIWQfPlowXVjeoY5qOpscD6HoTLMRCk9-nlLD2f0bq4zmkZrnud-uDszJvFUKT3dVbk9Zze3tFyURe0LNMLGrhcJo6Sb9OTCPz6ODnc6feZpXV4YLlANdjfyHrsPNgmJM2-g97iyRYMut8HOOiUfmdfe_n2bVZ2XRTpVV47s0CjGXqbjPBzWt7NFq6a0ZsFzf_FGPRKLlGHu7A7AR-AAjmIB9S150kqIisSebiayxZ_JUfh_h7Hf_OYTCb7O8vwv4NiVtjwjjebaeGdO5snENwYLpcQPGCvfkbg7WBakRD2G5_pp8bn60e-z26NbCQiArVgvHU39Hn0rIh9RIEVid2yxY45gIpU8sVJh1XLLNKWW6VJ3LHeYETYYFX5JBsSWz3gVrS55W8q9E3F-inwL8LLK_5wSuk" target="_blank" rel="noreferrer"><img src="https://mermaid.ink/img/pako:eNqllF9L5DAUxb_KJU8t1EGd9aVSobZBZKf-mY4-FUpsb8dAk8wmKbsifnfTzIzuuLusOH0o4XLuOT-SmzyTRrVIYmLwx4CywZyzpWbitJLgvhXTljd8xaSFi6z8s5hrJXFdvlIWQfPlowXVjeoY5qOpscD6HoTLMRCk9-nlLD2f0bq4zmkZrnud-uDszJvFUKT3dVbk9Zze3tFyURe0LNMLGrhcJo6Sb9OTCPz6ODnc6feZpXV4YLlANdjfyHrsPNgmJM2-g97iyRYMut8HOOiUfmdfe_n2bVZ2XRTpVV47s0CjGXqbjPBzWt7NFq6a0ZsFzf_FGPRKLlGHu7A7AR-AAjmIB9S150kqIisSebiayxZ_JUfh_h7Hf_OYTCb7O8vwv4NiVtjwjjebaeGdO5snENwYLpcQPGCvfkbg7WBakRD2G5_pp8bn60e-z26NbCQiArVgvHU39Hn0rIh9RIEVid2yxY45gIpU8sVJh1XLLNKWW6VJ3LHeYETYYFX5JBsSWz3gVrS55W8q9E3F-inwL8LLK_5wSuk?type=png" alt="Mermaid Sequence: Requesting AVAILABLE MODES"></a></p><p>In addition to the fields for enumerating the available modes, <code>AVAILABLE_MODES</code> has the following fields:</p><ul><li><code>standard_mode</code>: The standard mode represented by this <code>AVAILABLE_MODES</code> (<a href="#MAV_STANDARD_MODE">MAV_STANDARD_MODE</a>). <code>0</code> for a custom mode that is not also a standard mode (<code>MAV_STANDARD_MODE_NON_STANDARD</code>).</li><li><code>custom_mode</code>: The id of the custom mode represented by this <code>AVAILABLE_MODES</code> (if any).</li><li><code>properties</code>: Flags indicating the UI properties of this mode. These provide hints about where the mode can/should appear in the UI. For example, <code>MAV_MODE_PROPERTY_NOT_USER_SELECTABLE</code> indicates that the mode should not be set by a user, and hence should not appear in selection lists, while <code>MAV_MODE_PROPERTY_ADVANCED</code> indicates a mode that is harder to fly, and might be visually separated from other modes in the UI.</li><li><code>mode_name</code>: Mode metadata key or name, with null termination character. <ul><li>The field can act as both a key for determining metadata associated with the mode, or as a fallback name to use for the mode if the GCS has no metadata.</li><li>It can be used to override metadata on existing modes, for example to enhance standard metadata with additional information about autopilot-specific behaviour, or can provide metadata for any other static or dynamic mode.</li><li>The field must be human readable and autopilot-unique.</li><li>Generally does not have to be set for standard modes, where the ground station might be expected to already have metadata (but if it is, it will be used as a metadata key).</li><li>For more information see <a href="#modes-metadata">Modes Metadata</a> below.</li></ul></li></ul><h3 id="setting-modes" tabindex="-1">Setting Modes <a class="header-anchor" href="#setting-modes" aria-label="Permalink to &quot;Setting Modes&quot;">​</a></h3><p>Standard modes can be set using the <a href="#MAV_CMD_DO_SET_STANDARD_MODE">MAV_CMD_DO_SET_STANDARD_MODE</a> command. This will ACK with <code>MAV_RESULT_ACCEPTED</code> if the mode can change, and <code>MAV_RESULT_FAILED</code> if the particular standard mode is not supported.</p><p>Custom modes must be set with <a href="./../messages/common.html#MAV_CMD_DO_SET_MODE">MAV_CMD_DO_SET_MODE</a>.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If both <code>standard_mode</code> and <code>custom_mode</code> are set you can determine the mapping between modes and might therefore use <a href="./../messages/common.html#MAV_CMD_DO_SET_MODE">MAV_CMD_DO_SET_MODE</a> to set a standard mode. <code>MAV_CMD_DO_SET_STANDARD_MODE</code> is preferred for setting standard modes.</p></div><h3 id="getting-current-active-mode" tabindex="-1">Getting Current Active Mode <a class="header-anchor" href="#getting-current-active-mode" aria-label="Permalink to &quot;Getting Current Active Mode&quot;">​</a></h3><p>The current active mode is provided in <a href="#CURRENT_MODE">CURRENT_MODE</a>, which has both standard mode and custom mode fields.</p><p>This is streamed (nominally 0.5 Hz). A GCS can use it to configure the UI and display appropriate metadata for the current mode.</p><p>The message also has an <code>intended_custom_mode</code> field, which indicates the last (custom) mode that was commanded. This should match the <code>custom_mode</code> but might not if a commanded mode cannot be entered, or if the mode is exited due to a failsafe.</p><p>Note that the current custom mode is also published in the <a href="https://mavlink.io/en/messages/common.html#HEARTBEAT" target="_blank" rel="noreferrer">HEARTBEAT</a>.</p><h3 id="dynamic-mode-changes" tabindex="-1">Dynamic Mode Changes <a class="header-anchor" href="#dynamic-mode-changes" aria-label="Permalink to &quot;Dynamic Mode Changes&quot;">​</a></h3><p>The <a href="#AVAILABLE_MODES_MONITOR">AVAILABLE_MODES_MONITOR</a> is an optional part of the protocol that allows a MAVLink system to dynamically change the set of modes it supports at runtime (for example, using Lua or other onboard scripting languages, or offboard from a companion computer).</p><p>The message sequence number field is iterated sequentially whenever the set of available modes change. A GCS can detect this change and re-request the set of <a href="#AVAILABLE_MODES">AVAILABLE_MODES</a>.</p><p>The message should be emitted when the set of available modes changes. After the set of modes changes the first time, it should also be streamed at low rate (nominally 0.1 Hz).</p><h2 id="mode-metadata" tabindex="-1">Mode Metadata <a class="header-anchor" href="#mode-metadata" aria-label="Permalink to &quot;Mode Metadata&quot;">​</a></h2><p>Mode metadata is the information that a ground station uses represent a mode in the UI. This includes the mode name, but might also include descriptions, translations and other information (mode metadata schema is yet to be defined).</p><p>A GCS may already have knowledge of standard or custom modes and can provide this metadata based on the mode, or their might be particular &quot;additional&quot; information that it can provide. The <code>mode_name</code> can be used as a key for finding this metadata, or as a fallback string if the GCS has no metadata for the mode.</p><p>Ground stations are expected to use the following &quot;fallback&quot; approach for matching modes to their metadata:</p><ol><li><code>mode_name</code> field as a metadata key. If this field is defined and can be matched to metadata, the metadata should be used for the mode.</li><li><code>standard_mode</code>: Use metadata associated with this standard mode if no metadata can be matched using <code>mode_name</code>.</li><li><code>custom_mode</code>: Use metadata associated with the custom mode if no metadata can be matched using <code>mode_name</code> or <code>standard_mode</code>.</li><li><code>mode_name</code> field as a fallback mode name string. This should be used as the name if no other metadata can be mapped.</li></ol><p>This allows &quot;full&quot; autopilot-specific metadata to be provided for modes, including dynamic modes such as Lua scripts (where the <code>custom_mode</code> might be dynamically allocated). It also allows overriding of any existing metadata for any mode with vehicle-specific data. In the worse case, if there is no metadata for a custom mode, the <code>mode_name</code> string can still be used to represent the mode in the UI.</p><p>Note that the <code>mode_name</code> must be human readable and unique for the autopilot.</p><h2 id="implementations" tabindex="-1">Implementations <a class="header-anchor" href="#implementations" aria-label="Permalink to &quot;Implementations&quot;">​</a></h2><p>PX4 v1.15 and later supports this service, including the optional part (<code>AVAILABLE_MODES_MONITOR</code>) (this allows ROS2 modes added using the <a href="https://docs.px4.io/main/en/ros2/px4_ros2_interface_lib.html" target="_blank" rel="noreferrer">PX4 ROS 2 Interface Library</a> to be dynamically updated in a GCS). At time of writing it only exposes the standard modes that are common to all vehicles as standard modes, such as takeoff and landing, and missions.</p><p>QGC also uses this service to build the mode display/setting UI, when supported by the autopilot. This feature is supported in daily builds (at time of writing).</p>',52)]))}const f=a(i,[["render",s]]);export{u as __pageData,f as default};
