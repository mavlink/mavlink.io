import{_ as t,c as a,a8 as o,o as n}from"./chunks/framework.sDsl5pD6.js";const m=JSON.parse('{"title":"Tunnel Protocol","description":"","frontmatter":{},"headers":[],"relativePath":"en/services/tunnel.md","filePath":"en/services/tunnel.md"}'),r={name:"en/services/tunnel.md"};function i(s,e,l,d,c,u){return n(),a("div",null,e[0]||(e[0]=[o('<h1 id="tunnel-protocol" tabindex="-1">Tunnel Protocol <a class="header-anchor" href="#tunnel-protocol" aria-label="Permalink to &quot;Tunnel Protocol&quot;">​</a></h1><p>The <em>Tunnel Protocol</em> allows MAVLink to be used as a &quot;dumb channel&quot; to pass data in any format across, through or into a MAVLink network.</p><div class="warning custom-block github-alert"><p class="custom-block-title">WARNING</p><p><strong>Tunnels use is highly discouraged</strong>, as they lose most of the benefits of MAVLink in terms of efficiently and interoperability. You should almost always create &quot;dedicated&quot; MAVLink messages for communicating in a MAVLink system.</p></div><p>An example where this protocol is useful is connecting a <em>STorM32 gimbal</em> to a computer for configuration/debugging (via a USB connection to the autopilot and using MAVLink to carry the native serial protocol between it and the gimbal). Normally you would connect the gimbal directly via USB, but when mounted on the drone the gimbal USB port may be hard to access. You might re-invent the native protocol in MAVLink, but that would be a lot of effort for little benefit in this case.</p><p>The protocol consists of a single <a href="#TUNNEL">TUNNEL</a> message that has fields for the destination, payload length, payload type and data. The format of the data is entirely defined by the sender, and is inferred from the <code>TUNNEL.payload_type</code> (a <a href="#MAV_TUNNEL_PAYLOAD_TYPE">MAV_TUNNEL_PAYLOAD_TYPE</a>).</p><p>Vendors can use any value above 32767 for <code>MAV_TUNNEL_PAYLOAD_TYPE</code> in their own dialect during testing. For wider distribution they should <a href="#register">register the payload type</a>.</p><h2 id="message-enum-summary" tabindex="-1">Message/Enum Summary <a class="header-anchor" href="#message-enum-summary" aria-label="Permalink to &quot;Message/Enum Summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Message</th><th>Description</th></tr></thead><tbody><tr><td><span id="TUNNEL"></span><a href="./../messages/common.html#TUNNEL">TUNNEL</a></td><td>Message for transporting &quot;arbitrary&quot; variable-length data from one component to another (broadcast is not forbidden, but discouraged).</td></tr></tbody></table><table tabindex="0"><thead><tr><th>Enum</th><th>Description</th></tr></thead><tbody><tr><td><span id="MAV_TUNNEL_PAYLOAD_TYPE"></span><a href="./../messages/common.html#MAV_TUNNEL_PAYLOAD_TYPE">MAV_TUNNEL_PAYLOAD_TYPE</a></td><td>A code that identifies the format of the payload (0 for unknown, which is the default). You can use any value over 32768 for on private networks or <a href="#register">register a payload type</a>.</td></tr></tbody></table><h2 id="register" tabindex="-1">Register a Payload Type <a class="header-anchor" href="#register" aria-label="Permalink to &quot;Register a Payload Type {#register}&quot;">​</a></h2><p>The format of the tunnel message data is indicated by the value of <code>TUNNEL.payload_type</code>. Vendors can register their own formats in <a href="#MAV_TUNNEL_PAYLOAD_TYPE">MAV_TUNNEL_PAYLOAD_TYPE</a> by creating a PR to update the enum in <a href="https://github.com/mavlink/mavlink/blob/master/message_definitions/v1.0/common.xml" target="_blank" rel="noreferrer">common.xml</a>.</p><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>If you register an enum value in <strong>common.xml</strong> the MAVLink toolchain will generate an error if other dialect attempts to define the same value (if the dialect includes <strong>common.xml</strong>).</p></div><p>The rules for adding new <code>MAV_TUNNEL_PAYLOAD_TYPE</code> values are:</p><ol><li>Values 0-199 are reserved for MAVLink</li><li>Enum values are &quot;allocated&quot; to vendors and/or specific hardware in blocks of 10: <ul><li>New blocks must start on the decade boundary (e.g. 200-209, 210-219, etc.).</li><li>Blocks are allocated sequentially (in the previous point the next block is allocated at 220, not 430).</li><li>Vendors may reserve multiple sequential blocks if needed.</li></ul></li><li>Enum values in a block may be explicitly &quot;reserved&quot;: <ul><li>Values should be named using the format <code>MAV_TUNNEL_PAYLOAD_TYPE_XXXX_RESERVEDYY</code>, where <code>XXXX</code> indicates the vendor or software, and <code>YY</code> is an increasing number starting at 0. For example, the <em>STorM32 gimbal</em> might reserve a block of 10 values from <code>MAV_TUNNEL_PAYLOAD_TYPE_STORM32_RESERVED0</code> to <code>MAV_TUNNEL_PAYLOAD_TYPE_STORM32_RESERVED9</code>.</li><li>You do not <em>have to</em> explicitly reserve unused values.</li></ul></li><li>Enum values that are in use should be (re)named to reflect the vendor/software and purpose (e.g. <code>MAV_TUNNEL_PAYLOAD_TYPE_STORM32_CONFIG</code>).</li></ol>',14)]))}const p=t(r,[["render",i]]);export{m as __pageData,p as default};
